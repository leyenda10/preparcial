using UnityEngine;
using System.Collections;

public class cubo : MonoBehaviour {

	// Use this for initialization
	void Start () {
	// aqui va la info para inicar el juego
		gameObject.transform.position = new Vector3 (1, 1, 1);
	}
	
	// Update is called once per frame
	void Update () {
		}
}
